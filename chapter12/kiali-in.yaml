apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: kiali-ingress-2
  namespace: istio-system
spec:
  rules:
  - host: "kiali.nip.io"
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: kiali
            port: 
              number: 20001

